#include <stdio.h>

/*
What are the differences between a linked list and a stack?

A stack is a constrained linked list.  Whereas a linked list can have elements inserted and deleted at any point, a stack may only have elements inserted and deleted at the top.  A stack is a last in first out (LIFO) data structure.

*/

int main(void) {

}