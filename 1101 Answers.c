#include <stdio.h>

/*

A) Function fclose() closes a file.

B) The fscanf() function reads data from a file in a manner similiar to how scanf reads from stdin.

C) Function fgetc() reads a character from a specified file.

D) Function fgets() reads a line from a specified file.

E) Function fopen() opens a file.

F) Function fread() is normally used when reading data from a file in random access applications.

G) Function fseek() repositions the file position pointer to a specific location in the file.

*/
int main(void) {

}