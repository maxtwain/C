#include <stdio.h>

// This problem is not well described.  There is no input of anything.

int pay(int sales);

int main(void) {
	int record[10 - 2] = { 0 };
}

int pay(int sales) {
	return (int)(200.0 + 0.09 * sales);
}